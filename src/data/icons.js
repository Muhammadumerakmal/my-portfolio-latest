// Maps icon *names* used in portfolioData.js to Lucide React components.
// To use a new icon in the data file, import it here and add it to the map.
import { Zap, Bot, Rocket, Mail, ExternalLink, Server, Layers, Workflow, FolderGit2, Link2 } from 'lucide-react';

export const iconMap = {
  Zap,
  Bot,
  Rocket,
  Mail,
  ExternalLink,
  Server,
  Layers,
  Workflow,
  FolderGit2,
  Link2,
};
